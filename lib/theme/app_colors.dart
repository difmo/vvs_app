import 'package:flutter/material.dart';

class AppColors {
  // Core Brand Colors
  static const primary = Color(0xFFFF6F00); // Bhagwa Saffron (Header, Tabs)
  static const accent = Color(0xFFFF8F00); // Bright Saffron (CTA, Highlights)

  // Backgrounds
  static const background = Color(0xFFFFF3E0); // Warm Creamy Beige
  static const card = Color(0xFFFFFFFF); // White cards

  // Text Colors
  static const text = Color(0xFF4E342E); // Rich Brown
  static const subtitle = Color(0xFF6D4C41); // Softer brown

  // Borders & Dividers
  static const border = Color(0xFFD7CCC8); // Taupe / muted brown

  // Status Colors
  static const error = Color(0xFFD32F2F); // Red
  static const success = Color(0xFF388E3C); // Green

  // Extras for Cultural Theme
  static const maroon = Color(0xFF8B1C1C); // Deep Maroon (text highlights)
  static const gold = Color(0xFFFFC107); // Golden yellow (accents/badges)
  static const beige = Color(0xFFFFEBCD); // Soft beige background variant
}
