import 'package:flutter/material.dart';

class NewsBulletinScreen extends StatelessWidget {
  const NewsBulletinScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('News Bulletin Page')),
    );
  }
}
