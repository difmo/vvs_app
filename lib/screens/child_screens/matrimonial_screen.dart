import 'package:flutter/material.dart';

class MatrimonialScreen extends StatelessWidget {
  const MatrimonialScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('Matrimonial Page')),
    );
  }
}
